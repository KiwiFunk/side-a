//Header for the app. Include site name/logo to the left, and a profile icon to the right.
// The profile icon will be a dropdown menu with options to view profile, logout, etc.
// The header should be visible on all pages of the app.
// Pressing the logo takes us back to the homepage where a user can create a new mixtape

import React from 'react';

function Header() {
    return (
        //Placeholder styles, replace with CSS
        <header>
            <h1>Side A</h1>
        </header>
    );
}

export default Header;
